import random as r
import encodings as bcrypt

'''
def contraseña(tam):
    min = "abcdefghijklmnopqrstuvwxyz"
    may = min.upper
    car = "#$%_-@"
    num = "0123456789"
    all = 
    cad=""
    for x in range(0,tam):
        cad += r.choice(all)
    return cad
print(contraseña(10))'''

'''
def Encriptar_contrase(cadena):
    cadena = cadena.enconde('utf-8')
    cadena_encriptada = bcrypt.hashpw(cadena,bcrypt.gensalt())
print(contraseña(10))'''

'''
def Verificar_contra (contra,encryp):
    contra = contra.encode('utf-8')
    if bcrypt.checkpw(contra,encryp):
        return True
    else:
        return False'''
    
    

